<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_icon"
        android:label="Punch Card Widget"
        android:supportsRtl="true">

        <!-- Provider do widget -->
        <receiver
            android:name=".PunchCardWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.punchcardwidget.ACTION_TOGGLE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/punch_card_widget_info" />
        </receiver>

        <!-- Service que alimenta o GridView rolável dentro do widget -->
        <service
            android:name=".PunchCardRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- Tela de configuração exibida ao adicionar o widget -->
        <activity
            android:name=".PunchCardConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

    </application>
</manifest>
